// Route-level Suspense boundary for the dashboard.
// Enables immediate client-side navigation (no waiting for server response)
// and ensures Dashboard renders after the RSC transition is fully committed.
export default function DashboardLoading() {
  return null;
}
